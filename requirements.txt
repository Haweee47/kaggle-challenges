# ── 데이터 처리 ─────────────────────────────
pandas          # 표(DataFrame) 형태 데이터 조작의 표준 라이브러리
numpy           # 수치 연산 / 배열
scipy           # 통계 검정, 분포 (왜도 보정 등에 사용)

# ── 시각화 ─────────────────────────────────
matplotlib      # 모든 파이썬 시각화의 기반
seaborn         # matplotlib 래퍼. 통계 차트를 짧은 코드로

# ── 머신러닝 ───────────────────────────────
scikit-learn    # 전처리, 교차검증(KFold), 평가지표
lightgbm        # 정형 데이터 1순위 부스팅 모델 (빠름)
xgboost         # 부스팅 2번타자. 앙상블 다양성 확보용
catboost        # 범주형 처리에 강함. 앙상블 3번타자

# ── 튜닝 ───────────────────────────────────
optuna          # 베이지안 최적화 기반 하이퍼파라미터 튜닝

# ── 노트북 / 도구 ──────────────────────────
jupyter         # 노트북 실행 환경
ipykernel       # VS Code에서 .venv 커널 인식용
tqdm            # 반복문 진행률 표시줄
kaggle          # 대회 데이터 다운로드 / 제출 CLI
